version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=andemo-super-secret-key-change-me-in-prod-32chars
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
